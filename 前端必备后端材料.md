# MedShield-AI 前端必备后端材料

本文用于网页前端开发前快速定位后端材料、读取正确数据、避免误用旧实验口径。

## 1. 后端定位

`作品赛/MedShield-AI` 是一个 Python 实验后端，不是已经封装好的 Web API 服务。

前端应把最终正式实验输出作为只读数据源：

```text
C:\Users\18950843148\Desktop\作品赛\MedShield-AI\outputs\formal_competition\run_20260826_153334
```

页面可以展示 Step01-Step14 的完整后端结果；Step15 Dashboard 是当前要做的前端部分。

## 2. 最权威材料

| 材料 | 路径 | 前端用途 |
|---|---|---|
| 前端交接总文档 | `C:\Users\18950843148\Desktop\作品赛\# MedShield-AI 前端展示开发交接文档.txt` | 最高优先级说明，包含最终正式 run、seed、模型、展示边界 |
| Dashboard 数据契约 | `C:\Users\18950843148\Desktop\作品赛\MedShield-AI\docs\DASHBOARD_CONTRACT.md` | 解释 `summary.json` 和 `events.jsonl` 字段含义 |
| 系统能力边界 | `C:\Users\18950843148\Desktop\作品赛\MedShield-AI\docs\SYSTEM_CAPABILITIES.md` | 哪些能力可展示、哪些必须标注模拟/不可用 |
| 限制说明 | `C:\Users\18950843148\Desktop\作品赛\MedShield-AI\docs\LIMITATIONS.md` | 防止页面夸大真实医院部署能力 |
| 文件地图 | `C:\Users\18950843148\Desktop\作品赛\MedShield-AI\docs\FILE_GUIDE.md` | 查询后端目录和代码模块作用 |
| Schema 说明 | `C:\Users\18950843148\Desktop\作品赛\MedShield-AI\docs\schemas.md` | 理解事件、资产、风险、策略、恢复等数据结构 |
| 架构说明 | `C:\Users\18950843148\Desktop\作品赛\MedShield-AI\docs\ARCHITECTURE.md` | 做系统流程图、模块图、技术路线页 |
| 算法说明 | `C:\Users\18950843148\Desktop\作品赛\MedShield-AI\docs\UFN_SAR_ALGORITHM.md` | 解释 UFN-SAR + LightGBM，不误写为大模型 |

注意：部分 README 或 docs 仍保留 `NOT RUN` / `PENDING` 的旧文字。前端展示指标时，以最终正式 run 的真实 JSON 输出为准。

## 3. 前端必须读取的数据文件

最终正式 run 目录：

```text
C:\Users\18950843148\Desktop\作品赛\MedShield-AI\outputs\formal_competition\run_20260826_153334
```

| 文件 | 大小/特点 | 前端用途 |
|---|---:|---|
| `summary.json` | 小文件 | 首页总览卡片、核心指标、主要图表 |
| `metrics.json` | 中等 | 检测、场景、策略、防御、完整性、恢复、事件池、性能的细分图表 |
| `run_config.json` | 小文件 | seed、模型、数据规模、运行模式、能力边界 |
| `EXPERIMENT_RESULTS.md` | 小文件 | 比赛展示用文字摘要 |
| `behavior_manifest.json` | 小文件 | 行为事件总量、资产复用情况 |
| `results.csv` | 约 10MB | 事件表格、筛选、轻量 drill-down |
| `asset_manifest.json` | 约 17.5MB | 资产分布、资产详情、D1/D2/D3/Trap 展示 |
| `events.jsonl` | 约 665MB | 单事件完整链路；前端不要一次性全量加载，应分页、抽样或后端转接口 |

## 4. 推荐页面与对应数据

| 前端页面 | 主要读取 | 展示重点 |
|---|---|---|
| 总览页 | `summary.json` | 资产数、事件数、检测 F1、备份验证、遏制率、延迟 |
| 系统流程页 | 交接文档、`ARCHITECTURE.md` | Step01-Step14 流程图 |
| 数据资产页 | `summary.json`, `asset_manifest.json` | D1/D2/D3/Trap 资产分布、EMR/Image 类型 |
| AI 检测页 | `summary.json`, `metrics.json` | Precision、Recall、F1、AUC、Seen/Unseen |
| 场景分析页 | `metrics.json` | NORMAL、ACCOUNT_ABUSE、DATA_EXFILTRATION、EMR_LEAKAGE、IMAGE_LEAKAGE、MULTI_SOURCE_ATTACK、SLOW_DATA_EXFILTRATION |
| 策略防御页 | `summary.json`, `metrics.json` | P0-P3 分布、containment、false blocking、执行模式 |
| 完整性与恢复页 | `summary.json`, `metrics.json` | NORMAL/HIGH_CONFIDENCE_ANOMALY、Cold restore、post-normal |
| 事件列表页 | `results.csv` | event_id、asset_id、scenario、R、A、P、integrity、recovery、event_pool |
| 事件详情页 | `events.jsonl` | 单事件从行为到风险、策略、防御、完整性、恢复、反馈的完整链路 |
| 能力边界页 | `SYSTEM_CAPABILITIES.md`, `LIMITATIONS.md`, `run_config.json` | SIMULATED、PARTIAL、UNAVAILABLE、PIPELINE_LATENCY 等诚实说明 |

## 5. 首页可直接使用的关键数字

来自最终正式 run 的 `summary.json` 和 `metrics.json`：

```text
Protected Assets: 18,760
Behavior Events: 50,000
Normal Events: 35,000
Attack Events: 15,000
Seen Attack Events: 12,000
Unseen Attack Events: 3,000

Accuracy: 98.438%
Precision: 98.659%
Recall: 96.100%
F1: 97.362%
False Positive Rate: 0.560%
AUC: 98.609%

Seen Recall: 95.125%
Unseen Detection Rate: 100.000%
Attack Containment Rate: 96.100%
False Blocking Rate: 1.280%

Local Backup Verified: 18,760 / 18,760
Cold Backup Verified: 18,760 / 18,760
Recovery Requested: 2
Recovery Success Rate: 100.000%

Mean Pipeline Latency: 5.844 ms
P95 Pipeline Latency: 16.156 ms
Peak Memory: 1622.156 MB
```

## 6. `summary.json` 字段速查

| 字段 | 含义 |
|---|---|
| `schema_version` | 固定为 `dashboard-v1`，前端先校验 |
| `assets` | 唯一受保护资产，包含 D1/D2/D3/Trap |
| `behavior` | 行为事件总量与 Normal/Attack/Seen/Unseen 分布 |
| `backup` | Local/Cold 双备份创建和验证 |
| `detection` | Precision、Recall、F1、FPR |
| `seen_unseen` | 已见攻击召回、未见攻击检测率 |
| `policy` | P0/P1/P2/P3 数量，注意不是性能分数 |
| `defense` | 攻击遏制率、正常业务误阻断率 |
| `integrity` | 联合完整性四态分布 |
| `recovery` | 恢复请求数、成功率、恢复耗时、恢复来源 |
| `feedback` | 事件确认、入池、跳过、FP/FN、弱点报告 |
| `performance` | 平均耗时、P95 耗时、峰值内存 |

## 7. `results.csv` 列速查

`results.csv` 适合做事件表格，因为比 `events.jsonl` 轻很多。

| 列 | 含义 |
|---|---|
| `event_id` | 事件唯一 ID |
| `asset_id` | 资产 ID |
| `scenario` | 场景 |
| `ground_truth` | 真实标签 |
| `D` | D1/D2/D3 分类 |
| `Trap` | 是否诱捕资产 |
| `R` | 风险分数 |
| `A` | 异常类型 |
| `P` | 策略等级 |
| `containment` | 遏制状态 |
| `integrity_state` | 完整性状态 |
| `recovery_status` | 恢复状态 |
| `recovery_source` | 恢复来源 |
| `effectiveness` | 评估结果 |
| `event_confirmed` | 事件是否确认 |
| `event_pool_decision` | APPEND/SKIP/PENDING |
| `total_pipeline_ms` | 单事件总耗时 |
| `error` | 错误信息 |

## 8. `events.jsonl` 使用建议

`events.jsonl` 每行是一个完整事件对象，字段非常全，适合事件详情页，不适合直接全量塞进浏览器。

详情页可展示这些模块：

```text
behavior_summary
risk
policy
defense
integrity
recovery
evaluation
event_confirmation
event_pool_status
runtime_feedback
```

建议前端策略：

```text
列表页读取 results.csv
点击某个 event_id 后，再从 events.jsonl 按 event_id 查询详情
若先做纯静态 Demo，可人工抽取 10-20 条代表事件作为 samples
```

## 9. 不能误写的能力边界

页面可以展示：

```text
AES-256-GCM 实验加密
Local + Cold 双备份
D1/D2/D3 数据分级
Trap 诱捕资产
UFN-SAR + LightGBM 轻量 AI 检测
P0-P3 策略分级
主动防御流程
联合完整性检测
Cold 恢复闭环
Event Pool 反馈闭环
dashboard-v1 数据契约
```

页面必须诚实标注：

```text
Permission/Network: SIMULATED
Key enforcement: PARTIAL
DICOM watermark runtime: UNAVAILABLE
EMR embedded watermark: UNAVAILABLE
Blind unknown attribution: UNAVAILABLE
TTD/TTC: PIPELINE_LATENCY
Dataset: Synthetic/Public Experimental Data
Real HIS/PACS integration: NOT IMPLEMENTED
```

不要写成：

```text
已接入真实医院 HIS/PACS
已真实冻结医院账号
已真实修改医院防火墙
已支持生产 DICOM 水印
已支持 EMR 嵌入式水印
支持未知泄露图像盲归因
TTD/TTC 是真实医院响应时间
数据来自真实患者生产环境
```

## 10. 旧实验与旧口径禁用

禁止把这些当作最终结果：

```text
Seed 20270111
Seed 20270113
run_20260826_130445
run_20260826_140349
```

最终有效正式 Seed：

```text
20270130
```

最终有效正式 run：

```text
run_20260826_153334
```

## 11. 前端接入建议

第一阶段建议做纯静态 Dashboard：

```text
public/data/summary.json
public/data/metrics.json
public/data/run_config.json
public/data/results.csv
```

第二阶段再做轻量后端接口：

```text
GET /api/summary
GET /api/metrics
GET /api/events?page=1&pageSize=50&scenario=NORMAL
GET /api/events/:eventId
GET /api/assets?page=1&pageSize=50
```

这样不会动到原始后端，也能避免浏览器直接加载 665MB 的 `events.jsonl`。

